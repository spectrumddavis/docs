---
title: 'Get single receipt'
openapi: 'api-reference/openapi.json GET /receipts/{id}'
---

Retrieves a single receipt by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/receipts/{id}`

---

## Receipt-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

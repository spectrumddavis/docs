---
title: 'Query receipts (paginated)'
openapi: 'api-reference/openapi.json GET /receipts'
---

Returns receipt records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/receipts`

---

## Receipt-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Query purchase order lines (paginated)'
openapi: 'api-reference/openapi.json GET /purchase-order-lines'
---

Returns purchase order line records using the standard [paginated query](/api-reference/common-paginated-query) pattern. Purchase order lines are the individual line items within a purchase order.

**Endpoint:** `GET /api/purchase-order-lines`

---

## Purchase order line-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

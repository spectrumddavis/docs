---
title: 'Get single purchase order line'
openapi: 'api-reference/openapi.json GET /purchase-order-lines/{id}'
---

Retrieves a single purchase order line by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/purchase-order-lines/{id}`

---

## Purchase order line-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

---
title: 'Query purchase tax codes (paginated)'
openapi: 'api-reference/openapi.json GET /purchase-tax-codes'
---

Returns purchase tax code records using the standard [paginated query](/api-reference/common-paginated-query) pattern. Tax codes define the VAT or sales tax rates applied to purchase transactions.

**Endpoint:** `GET /api/purchase-tax-codes`

---

## Purchase tax code-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

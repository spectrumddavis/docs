---
title: 'Get single purchase tax code'
openapi: 'api-reference/openapi.json GET /purchase-tax-codes/{id}'
---

Retrieves a single purchase tax code by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/purchase-tax-codes/{id}`

---

## Purchase tax code-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

---
title: 'Query addresses (paginated)'
openapi: 'api-reference/openapi.json GET /addresses'
---

Returns address records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/addresses`

---

## Address-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

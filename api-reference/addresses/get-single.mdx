---
title: 'Get single address'
openapi: 'api-reference/openapi.json GET /addresses/{id}'
---

Retrieves a single address by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/addresses/{id}`

---

## Address-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

## Error codes

See [API Reference introduction](/api-reference/introduction#error-codes) for standard error codes and error envelope format.

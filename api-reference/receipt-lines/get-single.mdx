---
title: 'Get single receipt line'
openapi: 'api-reference/openapi.json GET /receipt-lines/{id}'
---

Retrieves a single receipt line by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/receipt-lines/{id}`

---

## Receipt line-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

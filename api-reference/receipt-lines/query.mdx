---
title: 'Query receipt lines (paginated)'
openapi: 'api-reference/openapi.json GET /receipt-lines'
---

Returns receipt line records using the standard [paginated query](/api-reference/common-paginated-query) pattern. Receipt lines record what was actually received against a purchase order line.

**Endpoint:** `GET /api/receipt-lines`

---

## Receipt line-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Query business units (paginated)'
openapi: 'api-reference/openapi.json GET /business-units'
---

Returns business unit records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/business-units`

---

## Business unit-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Get single business unit'
openapi: 'api-reference/openapi.json GET /business-units/{id}'
---

Retrieves a single business unit by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/business-units/{id}`

---

## Business unit-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

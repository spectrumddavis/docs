---
title: 'Get single nominal code'
openapi: 'api-reference/openapi.json GET /nominal-codes/{id}'
---

Retrieves a single nominal code by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/nominal-codes/{id}`

---

## Nominal code-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

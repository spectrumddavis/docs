---
title: 'Query nominal codes (paginated)'
openapi: 'api-reference/openapi.json GET /nominal-codes'
---

Returns nominal code records using the standard [paginated query](/api-reference/common-paginated-query) pattern. Nominal codes are general ledger (GL) codes used to categorise expenditure within a business unit.

**Endpoint:** `GET /api/nominal-codes`

---

## Nominal code-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

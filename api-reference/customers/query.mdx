---
title: 'Query customers (paginated)'
openapi: 'api-reference/openapi.json GET /customers'
---

Returns customer records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/customers`

---

## Customer-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Get single supplier'
openapi: 'api-reference/openapi.json GET /suppliers/{id}'
---


Retrieves a single supplier by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/suppliers/{id}`

---

## Supplier-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

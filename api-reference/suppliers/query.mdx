---
title: 'Query suppliers (paginated)'
openapi: 'api-reference/openapi.json GET /suppliers'
---

Returns supplier records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/suppliers`

---

## Supplier-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Get single purchase order'
openapi: 'api-reference/openapi.json GET /purchase-orders/{id}'
---

Retrieves a single purchase order by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/purchase-orders/{id}`

---

## Purchase order-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.

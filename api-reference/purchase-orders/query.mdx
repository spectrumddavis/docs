---
title: 'Query purchase orders (paginated)'
openapi: 'api-reference/openapi.json GET /purchase-orders'
---

Returns purchase order records using the standard [paginated query](/api-reference/common-paginated-query) pattern.

**Endpoint:** `GET /api/purchase-orders`

---

## Purchase order-specific notes

No quirks — this endpoint follows the [standard paginated query pattern](/api-reference/common-paginated-query) exactly.

---
title: 'Get single purchase transaction'
openapi: 'api-reference/openapi.json GET /purchase-transactions/{id}'
---

Retrieves a single purchase transaction by its unique identifier using the standard [get single record](/api-reference/common-get-single-record) pattern.

**Endpoint:** `GET /api/purchase-transactions/{id}`

---

## Purchase transaction-specific notes

No quirks — this endpoint follows the [standard get single record pattern](/api-reference/common-get-single-record) exactly.
